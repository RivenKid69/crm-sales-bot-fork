# utils




## Domains

This module covers the following business domains:

- обработка данных
- асинхронные операции
- криптография, безопасность
- api
- криптография, безопасность, хэширование
- настройки
- загрузка файлов
- файловая загрузка, API, веб-приложения
- загрузка файлов, обработка изображений
- криптографические функции
- файловая загрузка
- утилиты
- криптография, хэширование



## Components

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/file-uploading.utils.ts:file

Этот код проверяет, является ли загруженный файл изображением, и переименовывает его с случайным суффиксом. Первый фильтр блокирует неизображения, второй создает уникальное имя файла.

**Purpose:** Обеспечивает безопасную загрузку изображений с уникальными именами файлов

**Key Behaviors:**
- Блокирует загрузку неизображений (jpg, jpeg, png, gif)
- Генерирует уникальные имена файлов со случайным кодом
- Сохраняет оригинальное имя файла перед случайным суффиксом
- Использует модуль path для работы с расширениями
- Возвращает ошибки при неправильных файлах

**Uses:** path

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:function:paginate

Эта функция помогает разделить данные на страницы, как в библиотеке или интернет-магазине. Она показывает, какие элементы выводить на текущей странице, и даёт ссылки на следующую и предыдущую страницы.

**Purpose:** Используется для отображения больших списков данных постранично, например, в приложениях с таблицами или списками товаров.

**Key Behaviors:**
- Разделяет данные на страницы
- Вычисляет номера страниц и диапазоны элементов
- Создаёт ссылки на следующую и предыдущую страницы
- Поддерживает отображение пустых списков
- Возвращает структурированный результат


---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/sha1.ts:function:sha1

Эта функция вычисляет SHA-1 хэш строки. Сначала она пытается использовать встроенный модуль crypto, а если это не получается, то реализует алгоритм SHA-1 вручную, преобразуя строку в байты и выполняя сложные операции для получения хэша.

**Purpose:** Используется для создания уникального хэша строки, который можно применять, например, для проверки целостности данных или хранения паролей в безопасной форме.

**Key Behaviors:**
- Вычисляет SHA-1 хэш строки
- Использует встроенный модуль crypto, если он доступен
- Реализует алгоритм SHA-1 вручную, если встроенный модуль недоступен
- Поддерживает UTF-8 кодировку
- Возвращает хэш в нижнем регистре

**Uses:** crypto

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:function:httpBuildQuery

Эта функция преобразует объект в строку запроса, как в URL. Например, из {name: 'John', age: 30} получается 'name=John&age=30'.

**Purpose:** Используется для создания строк запросов в URL, например, при отправке данных на сервер.

**Key Behaviors:**
- Преобразует объект в строку запроса
- Использует URLSearchParams для формирования строки
- Проста в использовании
- Подходит для передачи данных в API
- Работает с любыми объектами

**Uses:** URLSearchParams

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/file-uploading.utils.ts:function:imageFileFilter

Функция imageFileFilter проверяет, является ли загружаемый файл изображением (jpg, jpeg, png, gif). Если файл не изображение, возвращает ошибку. Функция editFileName переименовывает файл, добавляя случайную строку к оригинальному названию для уникальности.

**Purpose:** Обеспечивает валидацию и переименование файлов при загрузке изображений в приложение

**Key Behaviors:**
- Проверка расширения файла
- Генерация случайного префикса для имени файла
- Отказ в загрузке неподдерживаемых форматов
- Сохранение оригинального имени файла
- Использование модуля path для работы с путями

**Uses:** path

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:import:locutus/php/strings/substr

Этот код реализует несколько утилит: преобразование объекта в строку URL-параметров, задержка выполнения, генерация кода для удалённого устройства, преобразование HEX в бинарный формат и чтение содержимого файла. Использует встроенные модули Node.js и портированную из PHP функцию substr.

**Purpose:** Обеспечивает набор вспомогательных функций для работы с данными, файлами и API в Node.js приложениях

**Key Behaviors:**
- Преобразование объекта в строку URL-параметров
- Создание уникального кода с датой и ID пользователя
- Преобразование HEX-строки в бинарный формат
- Синхронное чтение содержимого файла
- Генерация пагинации для списков данных

**Uses:** fs, locutus/php/strings/substr

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:function:sleep

Функция sleep приостанавливает выполнение кода на заданное количество секунд, используя промис. Это как пауза в видео - код ждёт, пока время не истечёт, прежде чем продолжить работу.

**Purpose:** Используется для временной паузы в асинхронных операциях, например при ожидании ответа от сервера или синхронизации данных.

**Key Behaviors:**
- Приостанавливает выполнение на заданное время
- Возвращает промис для асинхронного ожидания
- Проста в использовании и понятна для новичков


---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/md5.ts:function:md5

Эта функция вычисляет хэш MD5 строки. Сначала пытается использовать встроенный модуль Node.js 'crypto', если это не получается, использует ручную реализацию алгоритма MD5 с помощью вспомогательных функций.

**Purpose:** Генерация хэша MD5 для проверки целостности данных или аутентификации

**Key Behaviors:**
- Вычисление хэша через встроенный модуль crypto
- Ручная реализация алгоритма MD5 с использованием вспомогательных функций
- Поддержка UTF-8 кодирования для обработки строк
- Обработка ошибок при использовании встроенных модулей
- Поддержка работы без внутренних зависимостей

**Uses:** crypto, utf8_encode

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/file-uploading.utils.ts:import:path

Этот код проверяет, является ли загруженный файл изображением, и создает уникальное имя для файла. Использует модуль 'path' для получения расширения файла.

**Purpose:** Обеспечивает фильтрацию изображений и генерацию уникальных имен при загрузке файлов в приложение

**Key Behaviors:**
- Проверка расширения файла (jpg, jpeg, png, gif)
- Генерация уникального имени файла с помощью случайного числа
- Использование модуля 'path' для работы с расширениями файлов

**Uses:** path

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:function:fileGetContents

Эта функция читает содержимое файла с диска и возвращает его как строку. Она использует встроенный модуль Node.js для работы с файловой системой.

**Purpose:** Нужна для получения данных из файла, например, для загрузки конфигураций или шаблонов

**Key Behaviors:**
- Синхронное чтение файлов с диска
- Возвращает содержимое в виде строки UTF-8

**Uses:** fs

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/md5.ts:file

Этот код реализует алгоритм MD5 для генерации хэш-суммы строки. Он сначала пытается использовать встроенный модуль Node.js 'crypto', а если это не удается, то вручную реализует алгоритм MD5 с помощью функций, имитирующих поведение оригинального алгоритма.

**Purpose:** Используется для создания хэш-сумм строк, например, для проверки целостности данных или хранения паролей в безопасной форме.

**Key Behaviors:**
- Генерация хэш-суммы MD5 для строки
- Использование встроенной библиотеки Node.js 'crypto' при наличии
- Ручная реализация алгоритма MD5 при отсутствии встроенной библиотеки
- Поддержка UTF-8 кодирования
- Работа с битовыми операциями и преобразованием данных

**Uses:** crypto, utf8_encode

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:import:fs

Этот код содержит набор утилитных функций для работы с файлами, строками, временем и пагинацией. Например, функция 'sleep' задерживает выполнение, 'fileGetContents' читает файлы, а 'paginate' помогает разбивать данные на страницы.

**Purpose:** Предоставляет удобные вспомогательные функции для обработки данных, файлов и пагинации в проекте.

**Key Behaviors:**
- Чтение содержимого файла
- Пагинация данных
- Задержка выполнения
- Форматирование даты
- Конвертация шестнадцатеричных строк в двоичные

**Uses:** fs, locutus/php/strings/substr

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/file-uploading.utils.ts:function:editFileName

Эта функция проверяет, является ли загруженный файл изображением, и если да, разрешает его загрузку. Также она меняет имя файла, добавляя случайные символы для уникальности.

**Purpose:** Используется для фильтрации и переименования изображений при их загрузке на сервер.

**Key Behaviors:**
- Проверяет, является ли файл изображением
- Запрещает загрузку неизображений
- Генерирует уникальное имя для файла
- Добавляет случайные символы к имени файла
- Используется в загрузчиках файлов на сервере

**Uses:** path

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:function:getPrefixForDeletedDeviceCode

Эта функция создает уникальный префикс для удаленного кода устройства, используя идентификатор пользователя, дату и код устройства. Это помогает идентифицировать удаленные коды в системе.

**Purpose:** Используется для генерации уникального идентификатора для удаленных кодов устройств, чтобы их можно было легко найти и обработать.

**Key Behaviors:**
- Форматирует дату в формате 'дд.мм.гггг'
- Создает строку вида 'EXPIRED_UserId_Дата_КодУстройства'
- Использует русский язык для форматирования даты
- Позволяет легко идентифицировать удаленные коды
- Принимает userId и deviceCode в качестве входных данных

**Uses:** Intl.DateTimeFormat

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/md5.ts:import:locutus/php/strings/substr

Этот код реализует алгоритм хеширования MD5. Сначала он пытается использовать встроенный модуль Node.js 'crypto', а если это не получается — выполняет ручную реализацию алгоритма MD5 с помощью функций, имитирующих поведение PHP.

**Purpose:** Обеспечивает возможность генерации MD5-хешей даже в средах, где отсутствует модуль 'crypto'.

**Key Behaviors:**
- Использует встроенный модуль 'crypto' для хеширования
- Реализует ручной алгоритм MD5 с помощью битовых операций
- Обрабатывает ошибки при использовании модуля 'crypto'
- Использует вспомогательные функции для работы с битами и хеширования
- Поддерживает совместимость с PHP через импорт функции substr

**Uses:** crypto, locutus/php/strings/substr

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:function:hex2bin

Преобразует шестнадцатеричную строку в двоичную, обрабатывая каждый парный символ. Если в строке есть недопустимые символы, возвращает false.

**Purpose:** Используется для преобразования данных между разными форматами кодирования, например при работе с бинарными файлами или криптографией.

**Key Behaviors:**
- Конвертация шестнадцатеричной строки в двоичную
- Проверка корректности символов в строке
- Возвращает false при наличии ошибок
- Использует функцию substr для извлечения частей строки
- Возвращает результат в виде строки

**Uses:** locutus/php/strings/substr

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/sha1.ts:file

Этот код создает хэш SHA-1 для строки. Сначала он пытается использовать встроенный модуль Node.js 'crypto', а если это не получается, то вручную реализует алгоритм SHA-1 через сложные операции с битами и числами.

**Purpose:** Позволяет безопасно генерировать хэши для проверки целостности данных или хранения паролей

**Key Behaviors:**
- Генерация хэша через встроенный модуль Node.js
- Ручная реализация алгоритма SHA-1
- Обработка ошибок при выполнении
- Кодирование строки в UTF-8
- Работа с битовыми операциями

**Uses:** crypto

---

### /home/corta/crm_sales_bot/wipon_pro_api_main/wipon-pro-api-main/src/common/utils/common.ts:file

Этот файл содержит набор вспомогательных функций для работы с данными, файлами и временем. Например, функция sleep приостанавливает выполнение на заданное время, а paginate помогает разбить данные на страницы, как в библиотеке.

**Purpose:** Обеспечивает общие утилиты для проекта, такие как формирование URL-параметров, работа с файлами, преобразование данных и пагинация.

**Key Behaviors:**
- Формирование URL-параметров из объекта
- Приостановка выполнения на заданное время
- Генерация префикса для удалённых кодов устройств с датой
- Преобразование шестнадцатеричных строк в двоичные
- Чтение содержимого файла и пагинация данных

**Uses:** fs, locutus/php/strings/substr

---


## Metrics

- **Entities:** 18
- **Total Lines:** 0

---

*Generated by Codebase-Analyzer*