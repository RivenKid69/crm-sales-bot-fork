# API Documentation

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/auth.api.ts:file

Этот файл содержит функции для работы с авторизацией через API. Он отправляет запросы на сервер для получения кода подтверждения, обмена кода на токен и получения данных пользователя.

**Purpose:** Обеспечивает базовую авторизацию через SMS-коды и получение пользовательских данных

### Behaviors

- Отправка запроса на получение кода подтверждения по номеру телефона
- Обмен кода подтверждения на API-токен
- Получение данных авторизованного пользователя
- Использование HTTP-методов POST и GET
- Работа с JSON-телом запросов

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/index.ts:import:@/store

Этот код настраивает маршрутизатор Vue.js, который проверяет аутентификацию пользователя и перенаправляет его по разным маршрутам. Использует store для управления состоянием пользователя и токеном доступа.

**Purpose:** Обеспечивает защиту маршрутов, требующих авторизации, и управляет перенаправлениями в зависимости от состояния пользователя.

### Behaviors

- Проверка токена аутентификации перед доступом к защищенным маршрутам
- Установка заголовка страницы в зависимости от маршрута
- Обработка параметров URL (например, activateMobile)
- Перенаправление на страницу авторизации при отсутствии токена
- Интеракция с Vuex store для получения данных пользователя

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/account/index.ts:import:@/api/account.api

Этот код настраивает Vuex-модуль для управления состоянием аккаунта. Он определяет мутации для изменения данных, геттеры для получения данных и асинхронные действия для загрузки информации с сервера.

**Purpose:** Обеспечивает централизованное управление данными аккаунта в приложении Vue.js с возможностью загрузки и обновления информации

### Behaviors

- Обновляет баланс и номер счета через мутации
- Позволяет получать данные аккаунта через геттеры
- Асинхронно загружает данные с сервера через actions
- Обрабатывает транзакции и их типы
- Использует TypeScript для типизации состояния

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/company.api.ts:function:updateCompanyRequest

Эти функции отправляют HTTP-запросы для работы с данными компании. getCompanyRequest получает данные, setCompanyRequest создаёт новые, updateCompanyRequest обновляет существующие.

**Purpose:** Позволяет управлять данными компании через API-запросы

### Behaviors

- Получение данных компании
- Создание новой компании
- Обновление информации о компании
- Использование HTTP-методов GET/POST
- Работа с централизованным API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/storeTypes.api.ts:function:getStoreTypesRequest

Эта функция отправляет GET-запрос на сервер для получения списка типов магазинов. Она использует централизованный инструмент для работы с API, что упрощает управление запросами и делает код более читаемым.

**Purpose:** Используется для получения данных о типах магазинов из внешнего источника, например, сервера.

### Behaviors

- Отправляет GET-запрос
- Использует централизованный API-инструмент
- Возвращает данные о типах магазинов

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/subscriptions/index.ts:import:../../initialState

Этот код настраивает Vuex-модуль для управления подписками. Он использует мутации для изменения состояния, геттеры для получения данных и действия для взаимодействия с API, например, для получения и покупки подписок.

**Purpose:** Управление подписками пользователей, включая их получение, покупку и отображение в приложении

### Behaviors

- Использует Vuex для управления состоянием
- Получает данные подписок через API
- Обновляет состояние через мутации
- Сортирует и фильтрует подписки по активности
- Использует конфигурационные данные для цен

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/routes.ts:file

Этот файл настраивает маршруты для Vue-приложения. Он определяет, какие страницы отображаются по определённым URL, использует разные макеты (EmptyLayout и MainLayout) и проверяет, авторизован ли пользователь для доступа к маршрутам.

**Purpose:** Управляет навигацией в приложении и защищает маршруты от неавторизованного доступа

### Behaviors

- Определяет маршруты для аутентификации (/auth) и главной страницы (/)
- Использует разные макеты для разных частей приложения
- Проверяет статус авторизации для доступа к маршрутам
- Использует динамический импорт для ленивой загрузки компонентов
- Устанавливает заголовки страниц через мета-данные

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/auth/index.ts:class:Actions

Этот код управляет состоянием авторизации в приложении. Он сохраняет данные пользователя (телефон, токен, информацию о магазине) в локальное хранилище и синхронизирует их с сервером через API. Похоже на сейф, где хранятся важные вещи, и есть кнопки для их добавления, удаления и проверки.

**Purpose:** Управляет данными пользователя и процессом авторизации в веб-приложении

### Behaviors

- Сохраняет телефон пользователя в локальное хранилище
- Получает токен авторизации через API
- Выполняет выход из аккаунта (очищает все данные)
- Загружает информацию о пользователе с сервера
- Отправляет код подтверждения на телефон

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:import:@/router

Этот код настраивает Vuex-модуль для управления состоянием компании, её данных и связанных с ней сущностей. Он использует мутации для обновления данных, геттеры для их получения и действия для взаимодействия с API.

**Purpose:** Обеспечивает централизованное управление данными компании и её связанными сущностями в приложении на Vue.js.

### Behaviors

- Обновление состояния компании через мутации
- Получение данных из API через действия
- Использование геттеров для доступа к данным
- Работа с локальным хранилищем для сохранения состояния
- Навигация по маршруту в зависимости от условий

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/account.api.ts:function:getAccountRequest

Эта функция отправляет GET-запрос на сервер по адресу /accounts, чтобы получить информацию о счёте. Она использует общую функцию request из модуля API.

**Purpose:** Позволяет получить данные о счёте пользователя для отображения в интерфейсе или дальнейшей обработки

### Behaviors

- Отправляет GET-запрос на эндпоинт /accounts
- Использует централизованную функцию request для HTTP-запросов
- Возвращает данные о счёте в формате, определённом сервером

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/request.ts:import:@/config/index

Этот код создает HTTP-клиент на основе axios для работы с API. Он автоматически добавляет токен авторизации в заголовки запросов и обрабатывает ошибки, показывая уведомления и перенаправляя на страницу входа при потере сессии.

**Purpose:** Обеспечивает безопасное взаимодействие с API, обработку ошибок и управление состоянием авторизации

### Behaviors

- Автоматическое добавление токена авторизации в запросы
- Показ уведомлений об ошибках сервера
- Перенаправление на страницу входа при 401 ошибке
- Использование Vuex для управления состоянием
- Конфигурация базового URL из файла настроек

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/auth/index.ts:import:../../initialState

Этот код на TypeScript реализует логику аутентификации в приложении Vue.js с использованием Vuex. Он сохраняет данные пользователя, такие как телефон, токен и информацию о устройстве, и взаимодействует с API для отправки кода подтверждения, получения токена и получения данных пользователя.

**Purpose:** Управление состоянием аутентификации и взаимодействие с API для авторизации пользователя

### Behaviors

- Сохранение данных пользователя в Vuex и localStorage
- Отправка кода подтверждения через API
- Получение токена аутентификации от сервера
- Выход из системы и очистка данных
- Получение информации о пользователе из API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/subscriptions.api.ts:function:getSubsRequest

Эти функции отправляют запросы на сервер для работы с подписками: получают список подписок, покупают новую и оформляют возврат средств. Они используют API для взаимодействия с системой подписок.

**Purpose:** Позволяют управлять подписками пользователя через сетевые запросы к серверу

### Behaviors

- Получение списка всех подписок
- Покупка новой подписки
- Оформление возврата средств
- Работа с сервером через HTTP-запросы
- Обработка данных подписок

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/account.api.ts:function:getTransactionsRequest

Эта функция отправляет GET-запрос на получение информации о счетах пользователя с сервера по адресу /accounts. Она использует общий API-клиент и возвращает промис с результатом.

**Purpose:** Позволяет получить данные о счетах для отображения в интерфейсе или дальнейшей обработки

### Behaviors

- Отправка GET-запроса на эндпоинт /accounts
- Использование общего API-клиента из модуля request
- Возврат промиса с данными о счетах
- Отсутствие параметров в запросе
- Работа с сервером через HTTP-протокол

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/routes.ts:import:vue-router

Этот код настраивает маршруты для Vue-приложения, определяя, какие страницы доступны по каким URL. Используются разные макеты (EmptyLayout и MainLayout) и динамически подгружаются компоненты страниц.

**Purpose:** Управление навигацией в приложении и защита маршрутов от неавторизованных пользователей

### Behaviors

- Определение маршрутов с привязкой к компонентам
- Использование разных макетов для разных страниц
- Защита маршрутов через meta-поля (requiresAuth, onlyLoggedOut)
- Ленивая загрузка компонентов через динамические импорты
- Группировка маршрутов по функциональным категориям

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/subscriptions/index.ts:import:@/config/rates

Этот код настраивает Vuex-модуль для управления подписками в приложении. Он определяет мутации для изменения состояния, геттеры для получения данных и действия для взаимодействия с API.

**Purpose:** Управление состоянием подписок в Vue-приложении через Vuex

### Behaviors

- Обновление состояния подписок через мутации
- Получение данных подписок через геттеры
- Загрузка подписок из API через действия
- Обработка покупки подписок с использованием API
- Форматирование данных подписок из ответа сервера

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/storeTypes.api.ts:file

Этот файл содержит функцию для получения списка типов магазинов из API. Он использует общую функцию запроса и отправляет GET-запрос на эндпоинт /store-types.

**Purpose:** Позволяет получить данные о типах магазинов из сервера для использования в приложении

### Behaviors

- Отправляет GET-запрос на сервер
- Использует централизованную функцию запроса
- Возвращает данные о типах магазинов
- Работает с API-эндпоинтами
- Сервисный слой для работы с данными

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/config/index.ts:file

Этот файл содержит настройки для подключения к API. Он использует переменную окружения VUE_APP_API_URL, если она не задана, то используется URL по умолчанию. Также указана версия API (v1).

**Purpose:** Предоставляет централизованные настройки для работы с внешним API в приложении

### Behaviors

- Использует переменные окружения для конфигурации
- Предоставляет резервный URL при отсутствии переменной окружения
- Указывает версию API
- Создаёт централизованный источник констант
- Прост в модификации для разных окружений

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/request.ts:import:axios

Этот код настраивает Axios для работы с API. Он добавляет токен авторизации в заголовки запросов и обрабатывает ошибки, показывая уведомления и логая пользователей при неавторизованных запросах.

**Purpose:** Создание универсального клиента для API-запросов с поддержкой аутентификации и обработкой ошибок

### Behaviors

- Автоматическое добавление токена авторизации в заголовки
- Показ уведомлений об ошибках сервера
- Автоматический логаут при статусе 401
- Использование конфигурации из файла config
- Интеграция с Vuex-хранилищем и Vue Router

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/request.ts:file

Этот код создает экземпляр Axios с настройками для отправки HTTP-запросов. Он добавляет токен аутентификации в заголовки и обрабатывает ошибки, например, показывает уведомления и перенаправляет пользователя на страницу входа при неавторизованном доступе.

**Purpose:** Обеспечивает удобный и безопасный способ отправки HTTP-запросов в приложении с обработкой ошибок и аутентификации.

### Behaviors

- Добавляет токен аутентификации в заголовки запросов
- Обрабатывает ошибки сервера и показывает уведомления
- Перенаправляет пользователя на страницу входа при неавторизованном доступе
- Использует настройки из файла конфигурации
- Интегрируется с хранилищем и маршрутизатором Vue

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/payment.api.ts:function:generateInvoice

Эта функция отправляет данные на сервер для создания счета-фактуры в формате PDF и возвращает файл как объект blob. Она использует POST-запрос к эндпоинту /pdf/invoice.

**Purpose:** Создание и скачивание PDF-счетов-фактур через API

### Behaviors

- Отправляет POST-запрос на сервер
- Обрабатывает ответ в формате blob (бинарный файл)
- Принимает пользовательские данные для генерации PDF
- Использует относительный путь к API
- Работает как утилитарная функция

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/payment.api.ts:file

Этот код отправляет данные на сервер для генерации счета-фактуры в формате PDF. Функция возвращает ответ как бинарный файл (blob), который можно сохранить или открыть в браузере.

**Purpose:** Позволяет создавать и скачивать PDF-счета для клиентов в платежных процессах

### Behaviors

- Генерация PDF-счетов
- Отправка POST-запросов на сервер
- Обработка бинарных ответов
- Использование в модулях оплаты
- Работа с API для документов

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/request.ts:import:@kyvg/vue3-notification

Этот код создает экземпляр axios с настройками для API-запросов. Добавляет заголовки авторизации и обрабатывает ошибки с уведомлениями пользователя.

**Purpose:** Обеспечивает централизованную отправку HTTP-запросов с аутентификацией и обработкой ошибок в веб-приложении

### Behaviors

- Добавляет токен авторизации в заголовки запросов
- Показывает уведомления об ошибках сервера
- Перенаправляет на страницу входа при потере авторизации
- Использует конфигурационные настройки приложения
- Обрабатывает разные типы ошибок от сервера

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/account.api.ts:import:@/api/request

Этот код импортирует функцию `request` из файла `@/api/request` и использует её для создания запросов к API. Он определяет две функции: `getAccountRequest` и `getTransactionsRequest`, которые отправляют GET-запросы на определённые URL.

**Purpose:** Этот код позволяет получать данные с сервера, такие как информация о счёте и транзакции, используя HTTP-запросы.

### Behaviors

- Отправка GET-запросов на сервер
- Получение данных о счёте
- Получение данных о транзакциях
- Использование общего метода `request` для взаимодействия с API
- Упрощение повторяющегося кода для запросов

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/company.api.ts:function:setCompanyRequest

Эти функции отправляют HTTP-запросы к серверу для работы с данными компании. getCompanyRequest получает данные, setCompanyRequest создаёт новые, updateCompanyRequest обновляет существующие.

**Purpose:** Позволяет взаимодействовать с сервером для управления информацией о компании

### Behaviors

- Получение данных компании из базы
- Создание новой компании
- Обновление информации о компании
- Использование HTTP-методов GET/POST
- Работа с JSON-данными

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/main.ts:import:axios

Этот код настраивает Vue-приложение, подключая роутер, хранилище, стили и библиотеки для работы с API и уведомлениями. Он также добавляет функционал маскировки ввода и интегрирует Axios для HTTP-запросов.

**Purpose:** Создаёт готовое Vue-приложение с поддержкой навигации, состояния, стилей, API-запросов и уведомлений.

### Behaviors

- Интеграция Vue с роутером и хранилищем
- Подключение стилей и CSS-файлов
- Использование Axios для HTTP-запросов
- Добавление маски ввода для полей формы
- Интеграция уведомлений в приложение

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/ugds.api.ts:file

Этот файл создает функцию для получения данных с сервера по адресу '/ugds' с помощью GET-запроса. Он использует вспомогательную функцию request для отправки запроса.

**Purpose:** Позволяет получить данные из API для отображения в приложении

### Behaviors

- Отправляет GET-запрос на сервер
- Получает данные с эндпоинта /ugds
- Использует централизованный API-клиент

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/subscriptions/index.ts:class:commit<K extends keyof

Этот код - Vuex-модуль для управления подписками. Он хранит данные о доступных тарифах, выбранной подписке и информации о чеке. Обрабатывает запросы к API для получения данных о подписках и покупки новых.

**Purpose:** Управление состоянием подписок в приложении, позволяя получать, выбирать и покупать тарифные планы

### Behaviors

- Получение данных о подписках из API
- Обновление состояния выбранных тарифов
- Фильтрация активных подписок
- Сортировка тарифов по дате окончания
- Обработка покупки подписок

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/routes.ts:import:@/layouts/EmptyLayout.vue

Этот код настраивает маршруты для Vue-приложения, используя разные макеты (EmptyLayout и MainLayout) и защищая некоторые страницы авторизацией. Он определяет пути для входа, верификации и личного кабинета.

**Purpose:** Управление навигацией и защита доступа к разным частям приложения в зависимости от статуса пользователя

### Behaviors

- Определение маршрутов с разными макетами
- Защита маршрутов через meta-поля (requiresAuth, onlyLoggedOut)
- Ленивая загрузка компонентов с помощью динамического импорта
- Использование разных компонентов для разных частей приложения
- Настройка заголовков страниц через meta-поля

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:file

Этот файл - Vuex-модуль для управления данными компании. Он загружает информацию из API, сохраняет её в состоянии, и позволяет получить доступ к данным через getters. Если лицензиат не найден, пользователь перенаправляется на другую страницу.

**Purpose:** Централизованное хранение и управление данными компании в приложении

### Behaviors

- Загрузка данных компании из API
- Сохранение данных в Vuex-состояние
- Использование localStorage для хранения флагов
- Перенаправление по условию
- Предоставление доступа к данным через getters

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/company.api.ts:file

Этот файл содержит функции для работы с API компании. Он позволяет получать данные компании, создавать новую компанию и обновлять информацию о существующей.

**Purpose:** Позволяет взаимодействовать с сервером для управления данными компании через HTTP-запросы

### Behaviors

- Получение данных компании по адресу /store
- Создание новой компании через POST-запрос
- Обновление информации компании через /store/update

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/subscriptions.api.ts:function:makeRefund

Эта функция отправляет POST-запрос на сервер для возврата средств, используя путь /subscription/refund и передавая данные, необходимые для обработки возврата.

**Purpose:** Используется для обработки возврата средств по подписке в проекте.

### Behaviors

- отправка POST-запроса
- работа с данными для возврата
- интеграция с API
- обращение к серверу
- обработка подписок

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/account/index.ts:import:vuex

Этот код настраивает Vuex-модуль для управления состоянием аккаунта и транзакций. Он определяет мутации для изменения состояния, геттеры для получения данных и экшены для загрузки информации с API.

**Purpose:** Управление состоянием приложения через Vuex для аккаунта и транзакций

### Behaviors

- Обновление состояния аккаунта через мутации
- Получение данных из состояния через геттеры
- Загрузка данных с API через экшены
- Типизация состояния и действий
- Обработка ответов от API и преобразование данных

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:import:@/api/company.api

Этот код импортирует типы и функции из библиотеки Vuex и API-модулей, чтобы управлять состоянием компании в приложении. Он определяет мутации, геттеры и действия для работы с данными компании, такими как информация о компании, UGDS, DGDS и типы магазинов.

**Purpose:** Этот код используется для управления состоянием компании в приложении, получая и сохраняя данные из API, и обеспечивая доступ к ним через Vuex.

### Behaviors

- Получение данных компании из API
- Сохранение данных компании в состоянии Vuex
- Управление UGDS, DGDS и типами магазинов
- Проверка наличия магазина и перенаправление пользователя
- Использование localStorage для хранения состояния

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/ugds.api.ts:import:@/api/request

Этот код импортирует функцию `request` из файла `@/api/request` и использует её для отправки GET-запроса на эндпоинт `/ugds`. Это позволяет получать данные с сервера.

**Purpose:** Используется для получения данных с сервера через API-запрос

### Behaviors

- отправка GET-запроса
- получение данных с сервера
- использование внешнего модуля
- простой и понятный синтаксис
- работа с API-эндпоинтами

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/subscriptions.api.ts:function:buySubsRequest

Эта функция отправляет запрос на покупку подписки, используя данные, переданные в неё. Она взаимодействует с сервером через API, чтобы выполнить операцию покупки.

**Purpose:** Используется для покупки подписки в приложении, например, в мобильном приложении или веб-сервисе.

### Behaviors

- отправляет POST-запрос на сервер
- принимает данные для покупки
- работает с API
- взаимодействует с сервером
- используется для обработки покупок

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/subscriptions/index.ts:import:vuex

Этот код настраивает Vuex-модуль для управления подписками. Он определяет мутации для изменения состояния, геттеры для получения данных и действия для работы с API.

**Purpose:** Управление состоянием подписок в приложении Vue с возможностью получения и покупки подписок

### Behaviors

- Обновление состояния подписок через мутации
- Получение данных подписок через API
- Фильтрация активных подписок по дате
- Сортировка подписок по статусу и дате окончания
- Связь с конфигурационными данными (цены, типы)

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/storeTypes.api.ts:import:@/api/request

Этот код импортирует утилиту для HTTP-запросов и создает функцию, которая отправляет GET-запрос на сервер для получения списка типов магазинов

**Purpose:** Позволяет получить данные о типах магазинов из backend-системы для отображения в интерфейсе

### Behaviors

- Отправка GET-запросов на сервер
- Получение данных о типах магазинов
- Использование относительного пути к API
- Работа с внешними API-ресурсами
- Формирование запросов в формате JSON

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/ugds.api.ts:function:getUgdsRequest

Эта функция отправляет GET-запрос на сервер по адресу '/ugds' с помощью импортированного объекта request. Она возвращает данные, полученные с этого эндпоинта.

**Purpose:** Используется для получения данных с сервера по специфическому URL в приложении

### Behaviors

- Отправляет GET-запрос
- Получает данные с сервера
- Использует централизованный API-клиент

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/company.api.ts:function:getCompanyRequest

Эти функции отправляют HTTP-запросы для работы с данными компании. getCompanyRequest получает данные, setCompanyRequest создаёт новые, updateCompanyRequest обновляет существующие.

**Purpose:** Позволяет управлять данными компании через API-запросы в приложении

### Behaviors

- Получение данных компании из сервера
- Создание новой компании
- Обновление информации о компании
- Использование HTTP-методов GET/POST
- Работа с JSON-данными

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/index.ts:import:./routes

Этот код настраивает маршрутизацию в Vue-приложении. Он проверяет аутентификацию, устанавливает заголовок страницы, обрабатывает параметры URL и перенаправляет пользователя в зависимости от состояния авторизации.

**Purpose:** Управление навигацией и защита маршрутов, требующих авторизации, в веб-приложении

### Behaviors

- Настройка маршрутов с использованием Vue Router
- Проверка наличия токена авторизации для доступа к защищенным маршрутам
- Установка заголовка страницы на основе метаданных маршрута
- Перенаправление на страницу авторизации, если пользователь не аутентифицирован
- Обработка параметров URL, таких как токен или флаг активации мобильного устройства

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/auth/index.ts:class:Mutations

Этот код управляет состоянием аутентификации в приложении. Он сохраняет данные пользователя (телефон, токен, устройство) в localStorage и взаимодействует с API для отправки кодов, получения токенов и выхода из системы.

**Purpose:** Обеспечивает хранение и управление данными аутентификации в веб-приложении на Vue.js

### Behaviors

- Сохраняет данные пользователя в localStorage
- Отправляет код подтверждения через API
- Получает токен аутентификации от сервера
- Выполняет выход из системы, очищая данные
- Загружает данные пользователя из API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/auth/index.ts:import:@/api/auth.api

Этот код создает модуль Vuex для управления состоянием авторизации. Он хранит данные пользователя, токены и выполняет действия, такие как отправка кода подтверждения и выход из системы.

**Purpose:** Управление состоянием авторизации и взаимодействие с API для аутентификации пользователей

### Behaviors

- Сохранение телефона и токенов в localStorage
- Отправка кода подтверждения через API
- Получение данных пользователя с сервера
- Выход из системы с очисткой состояния
- Работа с токенами и устройствами

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:import:@/api/dgds.api

Этот код настраивает Vuex-модуль для управления данными компании, UGDS, DGDS и типов магазинов. Он использует API-запросы для получения данных и сохраняет их в состоянии, а также перенаправляет пользователя при отсутствии необходимых данных.

**Purpose:** Управление данными компании и связанными с ней сущностями в приложении на Vue.js с использованием Vuex

### Behaviors

- Загрузка данных компании из API
- Обновление состояния при получении данных
- Перенаправление на страницу авторизации при ошибке
- Использование getter'ов для доступа к данным
- Работа с локальным хранилищем (localStorage)

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/account/index.ts:class:Actions

Этот код определяет Vuex-модуль, который управляет состоянием аккаунта и транзакций. Он использует мутации для изменения состояния, геттеры для получения данных и действия для асинхронной загрузки информации из API.

**Purpose:** Этот модуль нужен для хранения и управления данными аккаунта и транзакций в приложении на Vue.js с использованием Vuex.

### Behaviors

- Загружает данные аккаунта из API и сохраняет их в состоянии
- Загружает транзакции и обрабатывает их перед сохранением
- Использует мутации для обновления состояния
- Позволяет получать данные через геттеры
- Работает с Vuex для управления состоянием

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/utils/helpers.ts:import:axios

Этот код содержит две функции: `sleep`, которая приостанавливает выполнение на заданное время, и `downloadBlob`, которая позволяет скачивать файлы из ответа API, если данные пришли в виде Blob. Обе функции полезны для работы с асинхронными операциями и обработки файлов.

**Purpose:** Обеспечивает утилиты для временного ожидания и скачивания файлов из API-ответов

### Behaviors

- Приостановка выполнения кода на заданное время
- Скачивание файлов из ответа API в виде Blob
- Совместимость с различными браузерами (включая Internet Explorer)

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/auth/index.ts:import:@/store

Этот код реализует модуль Vuex для управления состоянием авторизации. Он сохраняет данные пользователя (телефон, токен, информацию о устройстве) в localStorage и взаимодействует с API для отправки кодов верификации, получения токенов и получения данных пользователя.

**Purpose:** Управление состоянием авторизации и взаимодействие с API для аутентификации пользователей

### Behaviors

- Сохранение телефона и токена в localStorage
- Отправка кода верификации через API
- Получение токена авторизации по коду
- Выход из системы с очисткой данных
- Загрузка данных пользователя из API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/auth/index.ts:class:ActionsPayload

Этот код - модуль Vuex для управления аутентификацией. Он сохраняет данные пользователя (телефон, токен, устройство) в состоянии и localStorage, взаимодействует с API для получения токенов и проверки кодов, а также обрабатывает выход из системы.

**Purpose:** Управление состоянием аутентификации и взаимодействие с API для авторизации пользователей

### Behaviors

- Сохранение данных пользователя в состояние и localStorage
- Отправка кода верификации через API
- Получение токена авторизации
- Выход из системы (очистка данных)
- Загрузка данных пользователя из API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/dgds.api.ts:function:getDgdsRequest

Эта функция отправляет GET-запрос на сервер по адресу /dgds и получает данные. Она использует встроенную функцию request для выполнения запроса.

**Purpose:** Используется для получения данных из API по маршруту /dgds, например, для отображения информации на странице

### Behaviors

- Выполняет GET-запрос на сервер
- Использует централизованный request-объект для HTTP-запросов
- Возвращает данные из маршрута /dgds

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/company.api.ts:import:@/api/request

Этот код импортирует функцию `request` из файла `@/api/request` и использует её для отправки HTTP-запросов на сервер. Он определяет три функции: `getCompanyRequest` для получения данных, `setCompanyRequest` для создания данных и `updateCompanyRequest` для обновления данных.

**Purpose:** Эти функции позволяют взаимодействовать с сервером для получения, создания и обновления информации о компании.

### Behaviors

- Получение данных с сервера
- Создание новых данных на сервере
- Обновление существующих данных на сервере
- Использование одного и того же метода `request` для всех операций
- Простой и понятный интерфейс для работы с API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:class:ActionsPayload

Этот код управляет данными компании в приложении Vue.js. Он загружает информацию из API, сохраняет её в состоянии, и позволяет получать данные через getters. Если лицензиата нет, пользователь перенаправляется на страницу авторизации.

**Purpose:** Обеспечивает централизованное хранение и управление данными компании, UGDS, DGDS и типами магазинов в приложении

### Behaviors

- Загружает данные компании из API и сохраняет их в состоянии
- Обновляет данные UGDS, DGDS и типы магазинов из API
- Использует getters для получения данных из состояния
- Перенаправляет пользователя на страницу авторизации, если лицензиата нет
- Работает с локальным хранилищем для сохранения флага наличия магазина

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/dgds.api.ts:file

Этот файл содержит функцию, которая отправляет GET-запрос на сервер по адресу /dgds. Она использует общую функцию request для выполнения HTTP-запросов.

**Purpose:** Позволяет получить данные с сервера по специфичному эндпоинту /dgds

### Behaviors

- Отправляет GET-запрос
- Использует централизованную функцию request
- Возвращает данные с эндпоинта /dgds

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:import:vuex

Этот код настраивает Vuex-модуль для управления данными компании. Он получает данные с API, сохраняет их в состоянии, обновляет локальное хранилище и перенаправляет пользователя при необходимости.

**Purpose:** Централизованное управление данными компании в приложении

### Behaviors

- Получение данных с API (компания, UGDS, DGDS)
- Обновление состояния Vuex
- Синхронизация с localStorage
- Перенаправление по маршрутам при ошибке
- Работа с типизированными действиями и мутациями

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/dgds.api.ts:import:@/api/request

Этот код импортирует функцию request из модуля API и создаёт новую функцию getDgdsRequest, которая отправляет GET-запрос на сервер по адресу /dgds. Это позволяет получать данные с сервера в приложении.

**Purpose:** Используется для получения данных с сервера по определённому URL в приложении.

### Behaviors

- Отправляет GET-запрос
- Использует импортированную функцию request
- Возвращает данные с сервера
- Простой и понятный способ взаимодействия с API
- Используется в компонентах для загрузки данных

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:import:@/api/storeTypes.api

Этот код настраивает Vuex-модуль для управления данными компании. Он определяет мутации для обновления состояния, геттеры для получения данных и действия для загрузки информации с API, например, данные компании, UGDS, DGDS и типы магазинов.

**Purpose:** Централизованное управление данными компании в приложении Vue с использованием Vuex

### Behaviors

- Загрузка данных компании с API
- Обновление состояния через мутации
- Условная навигация при отсутствии данных
- Получение и обработка данных UGDS, DGDS и типов магазинов
- Хранение данных в localStorage

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/subscriptions/index.ts:import:@/api/subscriptions.api

Этот код настраивает Vuex-модуль для управления подписками. Он использует мутации для изменения состояния, геттеры для получения данных и действия для загрузки и обработки информации с API.

**Purpose:** Обеспечивает работу с подписками в приложении, включая их загрузку, сортировку и отображение

### Behaviors

- Использует Vuex для управления состоянием
- Загружает данные подписок с API
- Сортирует подписки по активности и дате окончания
- Обновляет состояние при изменении подписок
- Позволяет получать информацию о текущей активной подписке

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/subscriptions.api.ts:file

Этот файл содержит функции для работы с подписками через API. Он позволяет получать список подписок, покупать их и оформлять возвраты.

**Purpose:** Обеспечивает взаимодействие с системой подписок через HTTP-запросы

### Behaviors

- Получение списка всех подписок
- Покупка подписки
- Оформление возврата средств
- Использование общего API-клиента
- Работа с данными подписок

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:import:@/api/ugds.api

Этот код настроен на управление состоянием в приложении Vue.js с использованием Vuex. Он определяет мутации для обновления данных компании, UGDS, DGDS и типов магазинов, а также действия для получения этих данных с сервера.

**Purpose:** Обеспечивает централизованное хранение и управление данными компании и связанными с ней сущностями в приложении

### Behaviors

- Обновление состояния через мутации
- Получение данных с сервера через API
- Использование getter'ов для доступа к данным
- Работа с роутером для перенаправления пользователей
- Хранение данных в localStorage

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/routes.ts:import:@/layouts/MainLayout.vue

Этот код настраивает маршруты для Vue-приложения, используя разные макеты (EmptyLayout и MainLayout) для разделения публичных и приватных страниц. Каждый маршрут имеет метаданные для управления доступом и заголовками страниц.

**Purpose:** Организовать навигацию между страницами приложения с защитой маршрутов и разделением на публичные/закрытые зоны

### Behaviors

- Определение маршрутов с разными макетами
- Использование динамического импорта для код-сплиттинга
- Настройка метаданных для защиты маршрутов
- Разделение маршрутов на авторизованные и неавторизованные

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/account/index.ts:class:MutationPayload

Этот код представляет собой модуль Vuex, который управляет состоянием аккаунта и транзакций. Он использует мутации для изменения данных, геттеры для их получения и действия для асинхронной загрузки информации из API.

**Purpose:** Упрощает управление состоянием аккаунта и транзакций в Vue-приложении с помощью Vuex.

### Behaviors

- Обновляет данные аккаунта (баланс и номер)
- Загружает транзакции из API
- Преобразует данные транзакций перед сохранением
- Позволяет получать данные через геттеры
- Использует асинхронные операции для взаимодействия с API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/auth.api.ts:import:@/api/request

Этот код отправляет запросы на сервер для проверки номера телефона и получения токена доступа, а также получает данные пользователя. Он использует функцию `request` для выполнения HTTP-запросов.

**Purpose:** Обеспечивает авторизацию пользователя через ввод номера телефона и кода подтверждения, а также получение информации о пользователе.

### Behaviors

- Отправка кода подтверждения на телефон
- Получение токена доступа после ввода кода
- Загрузка данных пользователя
- Использование HTTP-запросов для взаимодействия с сервером
- Работа с телефонным номером и кодом подтверждения

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/subscriptions.api.ts:import:@/api/request

Этот код импортирует функцию 'request' из модуля '@/api/request' и создает три функции для работы с подписками: получение всех подписок, покупка подписки и возврат средств. Каждая функция отправляет HTTP-запрос на определенный эндпоинт.

**Purpose:** Обеспечивает взаимодействие с API для управления подписками и платежами

### Behaviors

- Получение списка всех подписок
- Покупка подписки через API
- Обработка возврата средств
- Использование общего HTTP-клиента 'request'
- Работа с JSON-данными

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/request.ts:import:@/store

Этот код создает HTTP-клиент на основе axios с аутентификацией и обработкой ошибок. Он автоматически добавляет токен авторизации в заголовки запросов и показывает уведомления при ошибках, например, при неавторизованном доступе.

**Purpose:** Обеспечивает безопасное взаимодействие с API и обработку ошибок в веб-приложении.

### Behaviors

- Автоматическая вставка токена авторизации в заголовки
- Обработка ошибок сервера и отображение уведомлений
- Перенаправление на страницу входа при неавторизованном доступе
- Использование конфигурации из файла config
- Интеграция с Vuex для управления состоянием

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/payment.api.ts:import:@/api/request

Этот код импортирует функцию `request` из файла `@/api/request` и использует её для отправки POST-запроса на сервер, чтобы сгенерировать счёт-фактуру в формате PDF. В ответе сервера ожидается файл в формате blob.

**Purpose:** Создание и отправка запроса на генерацию PDF-счёта-фактуры с использованием API

### Behaviors

- Отправляет POST-запрос на сервер
- Генерирует PDF-файл счёта-фактуры
- Использует `responseType: 'blob'` для получения файла
- Принимает данные для генерации счёта
- Использует внешний API для выполнения операции

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/routes.ts:import:@/store

Этот код настраивает маршруты для Vue-приложения. Он определяет пути для страниц авторизации, личного кабинета и использует разные макеты для отображения контента.

**Purpose:** Управляет навигацией в приложении, защищает маршруты и загружает компоненты по мере необходимости

### Behaviors

- Настройка маршрутов для авторизации и личного кабинета
- Использование разных макетов для отображения контента
- Защита маршрутов с помощью метаданных (requiresAuth, onlyLoggedOut)
- Ленивая загрузка компонентов через динамические импорты
- Настройка заголовков страниц

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/auth.api.ts:function:getUserData

Эта функция получает данные пользователя, отправляя GET-запрос на сервер по адресу /user. Она использует уже авторизованный запрос для получения информации о текущем пользователе.

**Purpose:** Позволяет получить данные текущего пользователя из системы после авторизации.

### Behaviors

- Получает данные пользователя с сервера
- Использует GET-запрос для получения информации
- Работает после успешной авторизации
- Не требует параметров для вызова
- Использует общую API-функцию для запросов

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/account.api.ts:file

Этот файл содержит функции для отправки GET-запросов на сервер. getAccountRequest получает данные аккаунта, getTransactionsRequest получает список транзакций.

**Purpose:** Позволяет получить данные пользователя и его транзакции из backend-системы

### Behaviors

- Загрузка информации о пользователе
- Получение списка транзакций
- Использование общего API-клиента
- Формирование корректных URL-адресов
- Разделение логики запросов по модулям

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/auth.api.ts:function:getApiTokenRequest

Эта функция отправляет запрос на получение токена авторизации, отправляя номер телефона и код подтверждения на сервер. Она используется после ввода пользователями кода, полученного на телефон.

**Purpose:** Позволяет авторизовать пользователя после успешной проверки кода подтверждения

### Behaviors

- Отправляет POST-запрос на эндпоинт /auth
- Принимает номер телефона и код подтверждения
- Используется во время процесса входа в аккаунт
- Работает вместе с функцией отправки кода
- Возвращает токен для дальнейшей авторизации

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/main.ts:import:./router

Этот код подключает основные зависимости и настройки для Vue-приложения: маршрутизацию, хранилище состояния, стили, Axios для HTTP-запросов, маски ввода и уведомления. Он готовит приложение к запуску.

**Purpose:** Настроить и запустить Vue-приложение с необходимыми функциями и библиотеками.

### Behaviors

- Инициализация приложения Vue
- Подключение маршрутизации
- Интеграция хранилища состояния
- Настройка Axios для HTTP-запросов
- Добавление уведомлений и масок ввода

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/index.ts:import:vue-router

Этот код настраивает маршрутизацию в Vue-приложении. Он проверяет аутентификацию пользователя, устанавливает заголовок страницы и перенаправляет по маршрутам в зависимости от состояния авторизации.

**Purpose:** Управляет навигацией, защищает маршруты и обеспечивает корректное поведение приложения при входе/выходе пользователя

### Behaviors

- Проверка токена авторизации перед доступом к защищенным маршрутам
- Динамическое изменение заголовка браузера в зависимости от маршрута
- Обработка параметров URL (например, activateMobile)
- Перенаправление на страницу авторизации при отсутствии токена
- Сброс прокрутки к верху при переходе между страницами

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/auth/index.ts:class:MutationPayload

Этот код - часть Vuex-модуля для управления аутентификацией в Vue-приложении. Он сохраняет данные пользователя (телефон, токен, устройство) в localStorage и взаимодействует с API для получения токенов и проверки кодов.

**Purpose:** Управление состоянием аутентификации и авторизации пользователя в приложении

### Behaviors

- Сохранение данных пользователя в localStorage
- Обновление токена аутентификации через API
- Отправка кода подтверждения на телефон
- Выход из аккаунта (очистка данных)
- Получение данных пользователя из API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/auth.api.ts:function:sendVerificationCodeRequest

Эта функция отправляет запрос на получение кода подтверждения по телефону и затем получает токен доступа, используя номер телефона и код подтверждения. Также она получает данные пользователя.

**Purpose:** Используется для авторизации пользователя через телефон и получения его данных

### Behaviors

- Отправляет запрос на получение кода подтверждения
- Получает токен доступа после ввода кода
- Загружает данные пользователя
- Использует один и тот же эндпоинт для двух разных действий
- Просто и понятно работает с API

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/company/index.ts:class:Getters

Этот код определяет мутации, геттеры и действия для Vuex-модуля, который управляет данными компании, Ugds, Dgds и типами магазинов. Он загружает данные с API, сохраняет их в состоянии и обновляет интерфейс.

**Purpose:** Управление данными компании и связанными с ней сущностями в приложении на Vue.js с использованием Vuex

### Behaviors

- Загрузка данных компании из API
- Сохранение данных в состоянии Vuex
- Обновление интерфейса при изменении данных
- Навигация по роутам при отсутствии лицензиата
- Получение и обработка Ugds, Dgds и StoreTypes

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/store/modules/subscriptions/index.ts:class:

Этот код — Vuex-модуль для управления подписками. Он хранит данные о подписках, позволяет получать их из API, фильтровать активные подписки и управлять выбранным тарифом.

**Purpose:** Обеспечивает централизованное управление данными о подписках в приложении

### Behaviors

- Получение данных подписок из API
- Фильтрация активных подписок
- Установка выбранных тарифов
- Обновление данных о счетах
- Сортировка подписок по дате окончания

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/router/index.ts:file

Этот файл настраивает маршрутизацию в приложении Vue. Он определяет, какие страницы доступны, как переходить между ними, и проверяет, авторизован ли пользователь, чтобы открыть определённые страницы.

**Purpose:** Управляет навигацией в приложении и защищает некоторые маршруты, требующие авторизации.

### Behaviors

- Настройка маршрутов приложения
- Проверка авторизации перед переходом на защищённые страницы
- Установка заголовка страницы в зависимости от маршрута
- Обработка параметров URL, например, токена авторизации
- Перенаправление на нужные страницы в зависимости от состояния пользователя

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/utils/helpers.ts:function:sleep

Функция sleep приостанавливает выполнение кода на заданное количество миллисекунд, возвращая промис, который разрешается после паузы. Функция downloadBlob позволяет сохранить файл из ответа API, создавая временный ссылочный элемент в DOM.

**Purpose:** Используется для временной задержки операций и скачивания файлов из API-ответов

### Behaviors

- Приостанавливает выполнение кода на заданное время
- Возвращает промис для асинхронного ожидания
- Обрабатывает скачивание файлов из Axios-ответов
- Поддерживает старые версии IE через msSaveBlob
- Создаёт временные элементы DOM для скачивания

---

## /home/corta/crm_sales_bot/wipon_pro_main/wipon-pro-main/src/api/request.ts:import:@/router

Этот код создает экземпляр axios с настройками для работы с API, добавляет обработку токена аутентификации и обработку ошибок. При ошибке 401 происходит выход из системы и перенаправление на страницу входа.

**Purpose:** Обеспечивает централизованную работу с HTTP-запросами, аутентификацией и обработкой ошибок в приложении

### Behaviors

- Автоматическое добавление токена аутентификации в заголовки
- Обработка ошибок 500 и 401 с уведомлениями пользователя
- Использование конфигурации из файла config для базового URL
- Перенаправление на страницу входа при потере токена
- Отображение пользовательских сообщений об ошибках

---


---

*Generated by Codebase-Analyzer*